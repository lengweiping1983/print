# 安装与运行

## 方式一：直接打开

1. 解压项目
2. 双击 `index.html`
3. 用 Chrome 打开

## 方式二：本地静态服务

如果浏览器直接打开有权限限制，推荐用本地静态服务。

### Python 方式

```bash
cd fabric_tool_final
python3 -m http.server 8080
```

浏览器打开：

```text
http://localhost:8080
```

## 使用步骤

1. 上传小参考图
2. 上传整套总 mask PNG
3. 点击“生成大面料图”
4. 左下选择裁片
5. 在左侧单片预览里拖动，或用滑杆调节参数
6. 右侧查看整套回排结果
7. 导出整套预览，或导出全部裁片 PNG

## 建议输入

### 参考图
- PNG / JPG 都可
- 尽量清晰
- 尺寸不要太小

### 总 mask
- PNG
- 透明背景
- 裁片区域 alpha 清晰
- 建议一套一个文件

## 常见问题

### 1. 为什么没有拆出裁片？
通常是总 mask 没有透明度信息，或者裁片 alpha 太弱。建议输出成透明背景 PNG。

### 2. 为什么默认选中的不是我想要的那块？
当前按面积从大到小排序，默认选最大裁片。你可以在左下裁片列表切换。

### 3. 导出全部裁片为什么是多个文件？
当前版本为纯前端简化实现，会逐个触发浏览器下载。

